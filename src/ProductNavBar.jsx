// Vertical navigation bar for each type of products
import React from 'react';

class ProductNavBar extends React.Component {
  constructor(props) {
    super(props);
    this.divStyle = this.props.cssStyle;
    this.dataList = this.props.navData;
  }

  
}
